# sekai-core (hybrid layout)

Rust core remains at the repository root (Cargo project).
Python packages are under `py/` using modern `src/` layout.

## Build Rust
`cargo build`

## Install Python side (editable)
`pip install -e .\py`
